package com.example.unum.ads

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun AdMobBanner(modifier: Modifier = Modifier) {
    // Ads are intentionally disabled for this build.
}
